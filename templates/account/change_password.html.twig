{% extends "layouts/base.html.twig" %}

{% block title %}Change password{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto">
            <h1 class="my-5 text-center">Change password</h1>
            {{form_start(form)}}
            {{form_widget(form)}}
            <button class="btn btn-success btn-block" type="submit">Update your password</button>
            {{form_end(form)}}
        </div>
    </div>
{% endblock %}